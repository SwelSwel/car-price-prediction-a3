name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      
      - name: Install dependencies
        run: |
          pip install numpy scikit-learn joblib pytest matplotlib
      
      - name: Run tests
        run: |
          PYTHONPATH=${{ github.workspace }} python -m pytest tests/ -v

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and push Docker image
        run: |
          docker build -t swelswel/car-prediction-dash:a3.0 ./app
          docker push swelswel/car-prediction-dash:a3.0

      - name: Deploy to server
        run: |
          sudo apt-get install -y sshpass
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          cat > ~/.ssh/config << 'SSHCONFIG'
          Host *
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
          Host bazooka
            HostName bazooka.cs.ait.ac.th
            User st127401
          Host ml-brain
            HostName ml.brain.cs.ait.ac.th
            User st127401
            IdentityFile ~/.ssh/id_ed25519
            ProxyJump bazooka
          SSHCONFIG
          chmod 600 ~/.ssh/config
          export SSHPASS="${{ secrets.SSH_PASSWORD }}"
          sshpass -e ssh ml-brain "docker pull swelswel/car-prediction-dash:a3.0 && docker compose -f docker-compose.yaml down && docker compose -f docker-compose.yaml up -d"